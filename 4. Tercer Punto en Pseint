Algoritmo numeroInvertido

    Funcion obtenerNumeroInvertido(numero: Entero) devuelve Entero
        Definir numeroInvertido Como Entero
        numeroInvertido <- 0

        Mientras numero > 0 Hacer
            numeroInvertido <- numeroInvertido * 10 + numero MOD 10
            numero <- numero DIV 10
        Fin Mientras

        Devolver numeroInvertido
    Fin Funcion

    Proceso principal
        Definir numero Como Entero

        Escribir "Ingrese un número: "
        Leer numero

        Definir numeroInvertido Como Entero
        numeroInvertido <- obtenerNumeroInvertido(numero)

        Escribir "Número invertido: " + ConvertirATexto(numeroInvertido)
    Fin Proceso

Fin Algoritmo
